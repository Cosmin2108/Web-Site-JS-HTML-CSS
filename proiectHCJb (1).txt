HTML
-taguri semantice (header, nav, section, article, aside)
     div, span
, h1-h6, p, img, ol, ul, a,
     table(nu pentru aliniere!!!), iframe cu ancore atasate
     meta, base

CSS
-selectori: id, clasa, tag, elementContinut, elementCopil,
               al n-lea copil de tipul unui tag (:nth-of-type)
, pseudoclasa (:hover)									x
   -proprietati: width, height, color, background, text-align, font, vertical-align     x
		 border, padding, margin,  visibility,

-meniu drop-down
   -galerie de elemente figure continand  figcaption					x

   -responsive: %(width) , em (font-size), media-query (@media)                         x
   -position(relative, absolute, sticky),z-index                                        X
   -transform, transition, animation							x
   -display- inclusiv inline-block                                                      x
   -stilizare flexbox (display)								x



Js-codul va fi SEPARAT de cel HTML (in fisier extern sau  script in document dar nu inline-pe atribut eveniment!!!)

  -modificare  de proprietati								x
	
  -modificare  a stilului unui element							x

-modificare  eveniment (ex: el.onclick=..)						x
	
  -selectare dupa id,tag,clasa,selectorCSS						x

  -creare si stergere element (createElement si removeChild)
